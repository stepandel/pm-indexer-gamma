{
  "name": "prediction-markets-monorepo",
  "version": "1.0.0",
  "description": "Multi-platform prediction market indexer monorepo",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "typecheck": "turbo run typecheck",
    "start": "turbo run start",
    "db:setup": "docker-compose up -d postgres && sleep 5 && pnpm db:migrate && pnpm db:generate",
    "db:start": "docker-compose up -d postgres",
    "db:stop": "docker-compose down",
    "db:logs": "docker-compose logs -f postgres",
    "db:reset": "docker-compose down -v && pnpm db:setup",
    "db:migrate": "turbo run db:migrate",
    "db:migrate:deploy": "turbo run db:migrate:deploy",
    "db:migrate:reset": "turbo run db:migrate:reset",
    "db:push": "turbo run db:push",
    "db:pull": "turbo run db:pull",
    "db:generate": "turbo run db:generate",
    "db:studio": "turbo run db:studio"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/pg": "^8.15.5",
    "typescript": "^5.9.2",
    "turbo": "^1.13.0"
  },
  "dependencies": {
    "@prisma/client": "^6.17.1",
    "dotenv": "^17.2.2",
    "pg": "^8.16.3",
    "prisma": "^6.17.1"
  },
  "packageManager": "pnpm@8.15.0",
  "engines": {
    "node": ">=18",
    "pnpm": ">=8"
  }
}
